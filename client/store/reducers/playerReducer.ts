import {PlayerActionTypes, PlayerInterface} from "@/types/player";

const initialState:PlayerInterface = {

}

export const playerReducer = (state = initialState,action:PlayerActionTypes):PlayerInterface=>{

}